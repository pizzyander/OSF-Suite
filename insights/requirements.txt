fastapi
uvicorn[standard]
httpx
python-multipart
redis
asyncpg
sqlalchemy[asyncio]
boto3
slowapi
pypdf>=3.0.0
python-docx>=1.0.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
pgvector>=0.2.4

# CPU-only torch build — sentence-transformers depends on torch, and pip's
# default resolution grabs the CUDA-enabled build even on a CPU-only
# machine like this EC2 instance, bundling several GB of nvidia/cudnn
# libraries that never get used (no GPU exists here). This line MUST come
# before sentence-transformers so pip resolves torch from this index first,
# rather than pulling the default GPU build afterward.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

sentence-transformers>=2.7.0
websockets
prometheus-fastapi-instrumentator==7.0.0